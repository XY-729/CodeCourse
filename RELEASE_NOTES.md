# CodeCourse v0.4.7 · Windows 桌面版

把 GitHub 项目变成可以逐课阅读、提问和复习的学习课程。本版沿用正式版界面，不包含重绘实验、奶蛙素材或相关动画；本次不提供 Android APK。

## 本版更新

- 改进学习交互、教学索引和 AI 问题路由，让课程与项目代码的联系更清晰。
- 完善学习档案、概念识别和历史学习证据处理。
- 精简生成进度与阅读控制，保留原有主题和工作区操作方式。
- 发布流程仅构建 Windows 安装版和便携版，增加前端测试与重绘文件检查。

## 下载与安装

在本页 **Assets** 中选择一个文件即可：

| 文件 | 用途 |
| --- | --- |
| `CodeCourse-0.4.7-setup.exe` | Windows 安装版，按向导选择安装位置，适合日常使用 |
| `CodeCourse-0.4.7-portable.exe` | Windows 便携版，直接启动，适合先体验 |
| `CodeCourse-0.4.7-guide.md` | 可下载保存的本说明书 |
| `SHA256SUMS.txt` | 安装版和便携版的 SHA-256 校验值 |

`latest.yml` 和 `.blockmap` 是更新用的辅助文件，无需手动打开。GitHub 自动提供的 Source code 是源码，不是桌面安装包。桌面包已内置运行所需组件，无需另装 Node.js 或 Python。旧用户建议先在“更多 → 导出 CodeCourse 数据包”备份，再关闭应用并升级。

## 使用说明书

### 1. 配置模型 API

1. 启动 CodeCourse，点击顶部右侧 **更多（…）→ 设置**，找到 **模型 API**。
2. 在服务商的平台申请 API Key，并确认账号可以调用所选模型。API 用量由服务商计费，CodeCourse 不附赠额度。
3. 填写以下字段：

   | 字段 | 填写方法 |
   | --- | --- |
   | Provider | DeepSeek 选 `DeepSeek`；其他兼容 Chat Completions 的接口选 `OpenAI Compatible` |
   | Base URL | DeepSeek 可填 `https://api.deepseek.com`；其他服务按其文档填写，例如 `https://api.openai.com/v1` |
   | Model | 填服务商实际支持且账号可用的模型 ID；程序默认值为 `deepseek-flash`，如服务商不支持，请替换 |
   | API Key | 粘贴服务商提供的完整密钥，避免带入空格或换行 |

4. 勾选 **启用模型 API**，点击 **保存**。不要勾选“保存时清除已保存 Key”，除非你确实要删除密钥。
5. 点击 **测试**，确认这次少量 API 调用；显示“测试成功”后再生成内容。修改字段后应先保存再测试。

Base URL 填接口基础地址，不要填写服务商网页，也不要在末尾填写 `/chat/completions`，程序会自动追加。API Key 保存在本机；如果电脑配置了 `DEEPSEEK_API_KEY` 或 `GPL_LLM_API_KEY` 环境变量，它们会优先于界面中保存的 Key 生效。

### 2. 导入项目，或创建学习计划

- **学习开源项目**：打开左上方项目菜单，选择 **导入 GitHub 仓库**，填写仓库地址（例如 `https://github.com/用户名/仓库名`），提交后等待下载和扫描完成，再选中该项目。首次建议用体积较小、结构清晰的公开仓库。
- **学习一个技术主题**：在项目菜单选择 **新建学习计划**，按界面填写信息并创建；这种方式不需要 GitHub 仓库。

导入与本地扫描不会自动生成整套 AI 课件。它们负责准备项目资料，接下来再主动生成学习总纲和每一课的正文。

### 3. 生成学习总纲

1. 确认左上方已选中目标项目或学习计划。
2. 点击顶部 **生成 → 学习总纲**。
3. 仓库项目可以选择整个项目，或选择指定文件；选择指定文件时，先用 **选择文件** 勾选需要学习的源码。
4. 在“补充要求”中写下基础、目标和重点。例如：

   > 我会 Python 基础，但没有后端经验。请围绕这个项目讲清一次请求如何进入路由、调用服务并读写数据。先解释概念，再结合实际文件安排课程，每课只聚焦一个主题。

   学习计划需要填写“学习目标”，例如：

   > 掌握 C++ 智能指针与移动语义。我了解类和指针，希望从生命周期开始，逐步学习 unique_ptr、shared_ptr 和 move，每课包含例子与练习。

5. 点击 **生成总纲**，按界面提示确认；如果出现“生成总纲前问卷”，根据自己的基础和目标回答后提交。
6. 等待生成任务完成。课程目录会更新，先阅读总纲并确认学习顺序。

总纲是课程路线与章节安排，不代表每节课的完整正文都已生成。若方向不合适，可以修改要求后重新生成；继续前请留意界面中的重生成提示。

### 4. 生成并学习课程

1. 在课程目录选择想学习的一课，建议从第一课开始。
2. 点击 **生成 → 当前课件**，按需填写补充要求，例如“先给直观例子，再逐段解释相关源码，最后给两道自测题”。
3. 点击 **生成课件**，按提示确认，等待正文生成完成。
4. 阅读该课，使用 AI 助手追问不理解的概念；完成后更新学习状态，再进入下一课并生成课件。

如果“当前课件”不可用，请先在课程目录选中具体课程。对于源码文件，可使用“生成 → 粗略介绍 / 详细分析”了解文件；这两个入口与生成课程正文用途不同。生成和 AI 提问会使用已配置的模型额度，建议逐课生成。

### 5. 保存与继续学习

课程、问答和学习记录保存在本地，下次启动后选择原项目即可继续。使用 **更多 → 导出 CodeCourse 数据包** 备份；数据包不包含 API Key。导入数据包会替换当前设备的 CodeCourse 数据，请先备份并阅读确认提示。

## 常见问题

- **提示未启用模型或没有 Key**：检查启用开关，填写 Key 后点击保存，再测试。
- **401 / 403**：检查 Key 是否有效，以及账号是否有调用该模型的权限。
- **404 / 模型不存在**：核对 Base URL 和 Model；模型 ID 必须与服务商提供的一致。
- **429 / 余额不足**：检查服务商额度、余额和频率限制，稍后再试。
- **超时或网络错误**：先测试 API 连通性；仓库下载失败则检查 GitHub 是否可访问。大项目可以先选择少量文件生成总纲。
- **只有课程标题，没有正文**：先选择某一课，再按第 4 节生成当前课件。
- **生成结果不符合基础**：在补充要求中明确已有知识、希望学到什么、讲解深度及练习形式，再重新生成。

## 验证

本次发布前，本地前端 572 项测试、后端 353 项测试全部通过。安装包由正式版源码构建，重绘实验不在本次发布范围内。
